#include<stdio.h>
#include<fcntl.h>
#include<unistd.h>
int main()
{
char buf[100];
size_t n;
int fd1,fd2;
fd1=open("a8",O_RDONLY);
fd2=open("z8",O_CREAT,0655);
while((n=read(fd1,buf,sizeof(buf)))>0)
write(fd2,buf,n);
printf("%s\n",buf);
remove("a8");  
}